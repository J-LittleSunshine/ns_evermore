# ns_runtime production dependencies.
# Redis/Valkey test drivers live in requirements-runtime-test.txt until P08
# freezes the production StateStore driver contract. QUIC/WebTransport
# experiments remain deferred to P21.

-r requirements-common.txt

uvloop==0.22.1; platform_system != "Windows"
websockets==16.0
