# ns_runtime production dependencies.
# Redis and Valkey are production StateStore provider drivers. They remain
# dynamically loaded so importing ns_common does not create a client.

-r requirements-common.txt

uvloop==0.22.1; platform_system != "Windows"
websockets==16.0
redis==8.0.1
valkey==6.1.1
